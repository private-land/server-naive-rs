/target
# Nested target/ inside vendored patches (cargo build artifacts leak here
# when patches/<pkg> is built directly).
patches/**/target

# Dev TLS certificates
.cert/

# Local ACL config
acl.yaml

# Claude rules
CLAUDE.md
.claude/

# Docs
docs/superpowers/
docs/specs/

# Local test configs / scripts / keys (never commit)
singbox-test*.json
web-key.txt
scripts/
